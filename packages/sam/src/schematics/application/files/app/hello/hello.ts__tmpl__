import { lambda } from '@nx-aws/sam';

export const handler = lambda({
    environmentVariables: [],
    handler: async () => {
        return {
            statusCode: 200,
            body: JSON.stringify({ message: 'Welcome to <%= name %>!' }),
        };
    },
});
