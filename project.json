{
    "name": "workspace",
    "$schema": "node_modules/nx/schemas/project-schema.json",
    "targets": {
        "semver": {
            "executor": "@jscutlery/semver:version",
            "options": {
                "syncVersions": true
            }
        }
    },
    "tags": []
}
